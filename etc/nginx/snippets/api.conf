location /api/ {
  access_log /var/log/nginx/cr7/api.access.log;
  error_log  /var/log/nginx/cr7/api.error.log warn;

  proxy_set_header X-Real-IP       $remote_addr;
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

	rewrite ^ $request_uri;
	rewrite ^/api/(.*) $1 break;
	return 400;

  proxy_pass                       http://api/$uri;
}