import { When, Given, Then } from '@deepracticex/vitest-cucumber';


Given('wechat mini app', async () => {
});

When('first open', async () => {
});

Then('register as a new user', async () => {
  throw new Error('Not implemented yet');
});